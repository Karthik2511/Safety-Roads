# backend/

This folder represents the FastAPI service layout used by the pothole detection backend.

For portfolio sharing, the repository includes safe API and configuration examples in `../CODE_SAMPLES/` instead of the full inference pipeline.
