# src/

This folder mirrors the front-end application structure of the project.

The full implementation is intentionally not exposed here. Safe front-end examples are published in `../CODE_SAMPLES/` so the repository still demonstrates how the app is wired without revealing the private business logic.
